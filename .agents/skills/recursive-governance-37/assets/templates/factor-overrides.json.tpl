{
  "schema_version": "1.0.0",
  "overrides": {},
  "notes": [
    "Keys in overrides must be factor IDs from the installed factor-registry.json.",
    "Use overrides only for repository-specific implementation details, not to redefine canonical factor identities.",
    "Changes that weaken protected boundaries require review."
  ]
}
